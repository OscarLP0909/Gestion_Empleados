services:
  mongodb:
    image: mongo:7
    environment:
      MONGO_INITDB_DATABASE: gestion_empleados
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    restart: unless-stopped

volumes:
  mongo_data: